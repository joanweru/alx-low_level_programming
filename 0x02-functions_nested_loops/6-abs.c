#include "main.h"

/**
 * _abs - omputes the absolute value of an integer
 * @n: integer to be tested
 * Return: abs value of integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

