#include "main.h"

/**
 * print_alphabet - prints the alphabet, in lowercase
 * followed by a new line. #44
 * Return: always zero if code was success
 */

void print_alphabet(void)
{
	char l;

	for (l = 'a'; letter <= 'z'; l++)
		_putchar(l);

	_putchar('/n');

}
