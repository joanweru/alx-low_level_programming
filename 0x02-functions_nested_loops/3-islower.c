#include "main.h"

/**
 * _islower - checks for lowercase character.
 * @c: character to be tested
 * Return: return 1 if c is lowercase
 * 0, otherwise...
 */

int _islower(int c)
{
	if (c >= 'a' && a <= 'z')
		return (1);
	else
		return (0);
}

