#include "main.h"

/**
 * int _isupper - checks for uppercase characters
 * @c: character to be tested
 * Return: 1 if c is uppercase
 * 0 otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
